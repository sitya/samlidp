{% extends 'base.html.twig' %}
{% trans_default_domain "federation" %}

{% block body %}
    {{ form_start(form) }}
    <div class="ibox">
        <div class="ibox-title">
            <h1>{{ "index.federation_list_federation_edit"|trans|raw }}</h1>
        </div>
        <div class="ibox-content">
            {{ form_start(form) }}
            {{ form_row(form.name) }}
            {{ form_row(form.slug) }}
            {{ form_row(form.federationUrl) }}
            {{ form_row(form.metadataUrl) }}
            {{ form_row(form.contactName) }}
            {{ form_row(form.contactEmail) }}
            {{ form_row(form.idps) }}

            <div class="form-group"><label class="col-sm-4 control-label">{{ "index.federation_list_sps"|trans|raw }}</label><div class="col-sm-8 form-control-static">{{ federation.sps }}</div>
            <div class="form-group"><label class="col-sm-4 control-label">{{ "index.federation_list_lastchecked"|trans|raw }}</label><div class="col-sm-8 form-control-static">{{ federation.lastChecked|date('Y-m-d H:i:s') }}</div>
            </div>

            {{ form_widget(form) }}

            <div class="hr-line-dashed"></div>

            <div class="form-group">
                <div class="col-sm-6 col-sm-offset-2">
                    <a class="btn btn-white" href="{{ path('federation_index') }}">{{ "index.federation_list_backtothelist"|trans|raw }}</a>
                    <input type="submit" class="btn btn-primary" value="{{ "index.federation_save"|trans|raw }}" />
                </div>
            </div>

            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

